using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyPadIn : MonoBehaviour
{
    public int Id;
    //public static event Action<string, int> Rotated = delegate { };
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
